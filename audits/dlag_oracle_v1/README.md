# DLAG Oracle v1

No-training, architecture-realizable ceiling audit for frozen HQMR-v1. It
evaluates component-level deep/local alpha arbitration and final presence-gate
force-on counterfactuals without changing any model parameter.

The exact environment, data paths, commands, outputs, and result table are
filled after the server audit completes. `parameter_updates` must remain zero.
