# RSBR-v0 Stage -1R Artifact Manifest

This directory is the repository copy of the completed audit metadata and
report from:

`/home/duyanhong/experiments/RSBR_V0_STAGE1R_PARITY_6b6f7db/`

It contains the frozen contract, full run metrics, prediction/name hashes,
pairwise comparisons, diagnostic-mode results, same-process identity records,
merge-path records, worker logs, and the complete Markdown report.

The large/redundant per-run `predictions.npz` arrays are intentionally not
committed to Git. They remain on the 5090 server at the path above and are the
source of every reported pixel-difference count. The server artifact directory
occupies approximately 15 MiB; this metadata-only repository copy is about
1.3 MiB.

Final decision:

`RSBR_V0_PARITY_HARNESS_NONDETERMINISM_CONFIRMED`

No training, optimizer step, test evaluation, or LUAD access occurred.

