# Phase-2B1.9 approved pre-outcome contract

User confirmed the recommended contract on 2026-08-30. Specification: RDDR_Phase2B1_9_Symmetric_Adjudication_Guided_Directional_Transfer_Audit_v1.0.md.

- Pure A0 4e9a2887b220d17e27649d72a3d13f32b7ebe8f9; independent feature/rddr-phase2b19-directional-transfer, PR base baseline/official-a0. No original source changes.
- All3418 BCSS validation images, C0 Full25 seed42 SHA509c264ec2df3b7dd6628b227d088db48300a2bc101ef3496d34ea6525911579. Reuse immutable Phase2B1 native / Phase2B15 symmetric / Phase2B18 raw logits and q derivatives. Hashes enforced by runner before/after.
- q=JS(ps,pd)/ln2. mD=(Delta_sym>0); ties reject. Only UDT/RG/ADT primary and SDT secondary. All coefficient1, no convex teacher or shared-head probe this round. SDT uses relu(Delta) without normalization beyond specified loss denominator.
- RG: NumPy default_rng42, one stream in frozen image order, each image sample without replacement from ALL784 positions with exactly the same active count as ADT. No GT/q/class matching or redraw; masks saved. Foreground/subgroup rates are observations, not forced to match.
- Primary student conv2d(F28_raw, detached ic1 weights/bias); q/Delta/gate/deep detached. Temporary eval-mode upstream gradients only b4,b4_1..b4_5,bn45 (39 parameters); require aggregate convolution gradients, not just BN. No change to original training freeze policy.
- Inherit epsilon-inside-log KL/JS, eps1e-8. Main batch1 all784 loss positions; batch20 whole-batch denominator. Real BF16 network, FP32 loss/logit/q derivatives, FP64 diagnostic accumulation. Native28 foreground0-3 diagnostic denominators; exclude background4/ignore255 ONLY from diagnostics, never from loss.
- Frozen ps/pd/q tolerances <=1e-7, stored q retained; old raw logits and stored symmetric gate replay exactly. Full support recomputation <=1e-7; NEVER change near-zero stored gate. v=-gradient, exact tie-aware margin, independent dq/dLs graph. Rejected dM/dQ=0 included in primary all-denominator metrics. Active-only secondary.
- Reuse frozen Top20/Q edges/boundary<=7px. Image-balanced AUC is mean per-image AUC over images with both labels; remaining rates pooled. 10000 paired image bootstrap seed42, percentile95%CI, same draws for all contrasts.
- Gate F: point conditions as spec, plus at least one of three predeclared contrasts significant in ADT-favorable direction: ADT-RG all mean dM CI>0; ADT-RG Shallow-Win harm CI<0; ADT-RG Deep-Win benefit CI>0. No multiplicity correction requested; this is the preregistered OR gate, not family-wise significance. Strong flag uses the same F significance definition.
- Decision precedence: G fail -> ENGINEERING_NOGO; A fail -> REPRODUCTION_FAIL; any B/C/D/E fail -> ADJUDICATION_VALID_DIRECTIONAL_TRANSFER_UNSAFE; F fail -> NOT_BETTER_THAN_RANDOM_SELECTION; otherwise GO. Thresholds unchanged.
- Class power requires >=500 Deep-Win, >=500 Shallow-Win and >=30 dual-label images. Known class3 Shallow-Win418 -> UNDERPOWERED. This caveat does not silently change A-G but prohibits claiming proven class3 hierarchy safety even if aggregate GO.
- Fixed32 linspace and128 seed42 remaining random replay; first20 fixed images batch20 BF16, no step. Original fixed160 inference prediction hash before official background overwrite, state/BN/checkpoint unchanged. No optimizer object/checkpoint write/test/LUAD/train split/other seed/search.
- Required33 tests, full3418 finite, independent derivatives/statistics/bootstrap/decision verification, all specified CSV/JSON, full41-section Markdown, README and PR. New output directories only; preserve all prior assets. STOP even if GO. No Full25/lambda choice.
